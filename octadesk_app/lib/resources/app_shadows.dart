import 'package:flutter/material.dart';

class AppShadows {
  static const BoxShadow s100 = BoxShadow(blurRadius: 4, color: Color.fromRGBO(0, 0, 0, 0.1), offset: Offset(0, 4), spreadRadius: 0);
  static const BoxShadow s200 = BoxShadow(blurRadius: 20, color: Color.fromRGBO(0, 0, 0, .05), offset: Offset(0, 0), spreadRadius: 0);
}
